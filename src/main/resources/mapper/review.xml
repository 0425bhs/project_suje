<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.suje.dao.ReviewDAO">
	<insert id="addReview"
            parameterType="review"
            useGeneratedKeys="true"
            keyProperty="review_id">
        INSERT INTO reviews (user_id, product_id, order_item_id, rating, content)
        VALUES (#{user_id}, #{product_id}, #{order_item_id}, #{rating}, #{content})
    </insert>

    <select id="getReviewById"
            parameterType="int"
            resultType="review">
        SELECT r.*, p.name product_name, p.image_l
        FROM reviews r
        JOIN products p ON r.product_id = p.product_id
        WHERE r.review_id = #{review_id}
    </select>

    <select id="getWrittenReview"
            parameterType="int"
            resultType="review">
        SELECT r.*,p.name AS product_name,p.image_l,oi.option_name
        FROM order_items oi
        JOIN orders o ON oi.order_id = o.order_id
        JOIN reviews r ON oi.order_item_id = r.order_item_id
        JOIN products p ON oi.product_id = p.product_id
        WHERE o.user_id = #{user_id}
        AND o.status = 'DELIVERED'
        ORDER BY r.created_at DESC
    </select>

    <select id="getWritableReview"
            parameterType="int"
            resultType="review">
        SELECT
            oi.order_item_id,
            oi.product_id,
            p.name product_name,
            p.image_l,
            oi.option_name
        FROM order_items oi
        JOIN orders o ON oi.order_id = o.order_id
        LEFT JOIN reviews r ON oi.order_item_id = r.order_item_id
        JOIN products p ON oi.product_id = p.product_id
        WHERE o.user_id = #{user_id}
        AND o.status = 'DELIVERED'
        AND r.review_id IS NULL
        ORDER BY o.created_at DESC
    </select>

    <select id="getWrittenReviewCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
        FROM order_items oi
        JOIN orders o ON oi.order_id = o.order_id
        JOIN reviews r ON oi.order_item_id = r.order_item_id
        WHERE o.user_id = #{user_id}
        AND o.status = 'DELIVERED'
    </select>

    <select id="getWritableReviewCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
        FROM order_items oi
        JOIN orders o ON oi.order_id = o.order_id
        LEFT JOIN reviews r ON oi.order_item_id = r.order_item_id
        WHERE o.user_id = #{user_id}
        AND o.status = 'DELIVERED'
        AND r.review_id IS NULL
    </select>

    <select id="getMyReviewList"
            parameterType="int"
            resultType="review">
        SELECT r.*, p.name product_name, p.image_l
        FROM reviews r
        JOIN products p ON r.product_id = p.product_id
        WHERE r.user_id = #{id}
        ORDER BY created_at DESC
    </select>

    <select id="getLiveReviewList"
            parameterType="int"
            resultType="review">
        SELECT r.*, u.name user_name, u.nick_name, p.name product_name, p.image_l
        FROM reviews r
        JOIN users u ON r.user_id = u.user_id
        JOIN products p ON r.product_id = p.product_id
        ORDER BY created_at DESC
        LIMIT 10
    </select>

    <select id="getReviewListByKeyword"
            resultType="review">
        SELECT r.*, p.name product_name, u.name user_name
        FROM reviews r
        JOIN products p ON r.product_id = p.product_id
        JOIN users u ON r.user_id = u.user_id
        <where>
            <if test="user_id != null">
                AND r.user_id = #{user_id}
            </if>

            <if test="product_id != null">
                AND r.product_id = #{product_id}
            </if>

            <if test="review_id != null">
                AND r.review_id = #{review_id}
            </if>

            <if test="keyword != null and keyword != ''">
                AND (
                    p.name LIKE CONCAT("%", #{keyword}, "%")
                    OR u.name LIKE CONCAT("%", #{keyword}, "%")
                    OR r.content LIKE CONCAT("%", #{keyword}, "%")
                ) 
            </if>

            <if test="startDate != null and startDate != ''">
                AND r.created_at &gt;= CONCAT(#{startDate}, ' 00:00:00')
            </if>

            <if test="endDate != null and endDate != ''">
                AND r.created_at &lt;= CONCAT(#{endDate}, ' 23:59:59')
            </if>
        </where>
        <choose>
            <when test="sort == 'oldest'">
                ORDER BY r.created_at ASC, r.review_id ASC
            </when>
            <when test="sort == 'rating'">
                ORDER BY r.rating DESC, r.review_id DESC
            </when>
            <otherwise>
                ORDER BY r.created_at DESC, r.review_id DESC
            </otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="getReviewListCountByKeyword"
            resultType="int">
        SELECT count(*)
        FROM reviews r
        JOIN products p ON r.product_id = p.product_id
        JOIN users u ON r.user_id = u.user_id
        <where>
            <if test="user_id != null">
                AND r.user_id = #{user_id}
            </if>

            <if test="product_id != null">
                AND r.product_id = #{product_id}
            </if>

            <if test="review_id != null">
                AND r.review_id = #{review_id}
            </if>

            <if test="keyword != null and keyword != ''">
                AND (
                    p.name LIKE CONCAT("%", #{keyword}, "%")
                    OR u.name LIKE CONCAT("%", #{keyword}, "%")
                    OR r.content LIKE CONCAT("%", #{keyword}, "%")
                ) 
            </if>

            <if test="startDate != null and startDate != ''">
                AND r.created_at &gt;= CONCAT(#{startDate}, ' 00:00:00')
            </if>

            <if test="endDate != null and endDate != ''">
                AND r.created_at &lt;= CONCAT(#{endDate}, ' 23:59:59')
            </if>
        </where>
    </select>

    <update id="updateReview"
            parameterType="review">
        UPDATE reviews
        SET content = #{content},
            rating = #{rating}
        WHERE review_id = #{review_id}
    </update>

    <delete id="deleteReview"
            parameterType="int">
        DELETE FROM reviews
        WHERE review_id = #{review_id}
    </delete>

    <select id="getProductReviewList" resultType="review">
        SELECT
            r.review_id,
            r.user_id,
            r.product_id,
            r.order_item_id,
            r.rating,
            r.content,
            r.created_at,

            DATE_FORMAT(r.created_at, '%Y.%m.%d') AS created_at_text,

            r.reply_content,
            r.reply_created_at,
            DATE_FORMAT(r.reply_created_at, '%Y.%m.%d') AS reply_created_at_text,

            u.name AS user_name,
            u.nick_name,
            u.photo_name,

            oi.option_name AS option_name

        FROM reviews r

        JOIN users u
            ON r.user_id = u.user_id

        LEFT JOIN order_items oi
            ON r.order_item_id = oi.order_item_id

        WHERE r.product_id = #{value}

        ORDER BY r.created_at DESC
    </select>

    <select id="productReviewList" resultType="review">
        SELECT
            r.review_id,
            r.user_id,
            r.product_id,
            r.order_item_id,
            r.rating,
            r.content,
            r.created_at,

            DATE_FORMAT(r.created_at, '%Y.%m.%d') AS created_at_text,

            r.reply_content,
            r.reply_created_at,
            DATE_FORMAT(r.reply_created_at, '%Y.%m.%d') AS reply_created_at_text,

            u.name AS user_name,
            u.nick_name,
            u.photo_name,

            oi.option_name AS option_name

        FROM reviews r

        JOIN users u
            ON r.user_id = u.user_id

        LEFT JOIN order_items oi
            ON r.order_item_id = oi.order_item_id

        WHERE r.product_id = #{value}

        ORDER BY r.review_id DESC
    </select>

    <select id="getReviewCount" parameterType="int" resultType="int">
        SELECT count(*)
        FROM reviews
        WHERE user_id = #{user_id}
    </select>

    <select id="bestReview" parameterType="int" resultType="map">
        SELECT
            r.review_id,
            r.product_id,
            r.user_id,
            r.rating,
            r.content,
            DATE_FORMAT(r.created_at, '%Y-%m-%d') AS created_at,
            u.nick_name,
            u.name,
            IFNULL(ri.image_count, 0) AS image_count,
            ri.review_image

        FROM reviews r

        JOIN users u
            ON r.user_id = u.user_id

        LEFT JOIN (
            SELECT
                target_id AS review_id,
                COUNT(*) AS image_count,
                MIN(image_url) AS review_image
            FROM images
            WHERE target_type = 'REVIEW'
            GROUP BY target_id
        ) ri
            ON r.review_id = ri.review_id

        WHERE r.product_id = #{product_id}

        ORDER BY
            r.rating DESC,
            CHAR_LENGTH(r.content) DESC,
            IFNULL(ri.image_count, 0) DESC,
            r.created_at DESC

        LIMIT 3
    </select>

    <select id="sellerReviewList" resultType="review">
        SELECT
            r.review_id,
            r.user_id,
            r.product_id,
            r.order_item_id,
            r.rating,
            r.content,
            r.created_at,

            DATE_FORMAT(r.created_at, '%Y.%m.%d') AS created_at_text,

            r.reply_content,
            r.reply_created_at,
            DATE_FORMAT(r.reply_created_at, '%Y.%m.%d') AS reply_created_at_text,

            p.name AS product_name,
            p.image_l,

            u.name AS user_name,
            u.nick_name,
            u.photo_name,

            oi.option_name AS option_name

        FROM reviews r
        JOIN products p
            ON r.product_id = p.product_id
        JOIN users u
            ON r.user_id = u.user_id
        LEFT JOIN order_items oi
            ON r.order_item_id = oi.order_item_id

        WHERE p.seller_id = #{seller_id}

        ORDER BY r.created_at DESC
    </select>


    <select id="sellerReviewProductList" resultType="review">
        SELECT DISTINCT
            p.product_id,
            p.name AS product_name
        FROM products p
        JOIN reviews r
            ON p.product_id = r.product_id
        WHERE p.seller_id = #{seller_id}
        ORDER BY p.name ASC
    </select>

    <update id="sellerReviewReply" parameterType="map">
        UPDATE reviews r
        JOIN products p
            ON r.product_id = p.product_id
        SET
            r.reply_content = #{reply_content},
            r.reply_created_at = NOW()
        WHERE r.review_id = #{review_id}
        AND p.seller_id = #{seller_id}
    </update>

    <select id="getReviewCountByUserId" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM reviews
        WHERE user_id = #{user_id}
    </select>

</mapper>
