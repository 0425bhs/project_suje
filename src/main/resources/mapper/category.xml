<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.suje.dao.CategoryDAO">
	
    <select id="big_category_list" resultType="category">
        select category_id,name,parent_id from categories where parent_id IS NULL order by category_id
    </select>

    <select id="small_category_list" resultType="category">
        select category_id,name,parent_id from categories where parent_id=#{value} order by category_id
    </select>

    <select id="find_parent_id" resultType="java.lang.Integer">
        select parent_id from categories where category_id=#{value}
    </select>
    
    <select id="getCategroyNameById"
            resultType="String">
        select name from categories where category_id=#{value}
    </select>

    <select id="small_category_all_list" resultType="category">
        SELECT category_id,name,parent_id from categories
        where parent_id IS NOT NULL order by parent_id, category_id
    </select>
    
    <select id="getCategoryHierarchy"
            resultType="category">
        SELECT p.category_id parent_id, p.name parent_name, c.category_id child_id, c.name child_name
        FROM categories p
        LEFT JOIN categories c ON p.category_id = c.parent_id
        WHERE p.parent_id IS NULL
    </select>
</mapper>