<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.suje.dao.ReportDAO">
    <insert id="addReport">
        INSERT INTO reports (reporter_id, target_type, target_id, reason)
        VALUES (#{reporter_id}, #{target_type}, #{target_id}, #{reason})
    </insert>

    <select id="getReportListByKeyword" resultType="report">
        SELECT r.*, u.name reporter_name
        FROM reports r
        JOIN users u ON u.user_id = r.reporter_id
        <where>
        <if test="status != null and status != 'all'">
            r.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
                r.reason LIKE CONCAT("%", #{keyword} ,"%")
                OR u.name LIKE CONCAT("%", #{keyword} ,"%")
            )
        </if>
        </where>
    </select>

    //임시 제미나이
    <!-- <select id="getReportListByKeyword" resultType="report">
        SELECT 
            r.*, 
            u.name AS reporter_name,
            COALESCE(p.product_name, rv.review_title, tu.name) AS target_name
        FROM reports r
        JOIN users u ON u.user_id = r.reporter_id
        LEFT JOIN product p ON r.target_type = 'PRODUCT' AND r.target_id = p.product_id
        LEFT JOIN review rv ON r.target_type = 'REVIEW' AND r.target_id = rv.review_id
        LEFT JOIN users tu ON r.target_type = 'USER' AND r.target_id = tu.user_id
        <where>
            <if test="status != null and status != 'all'">
                r.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    r.reason LIKE CONCAT('%', #{keyword}, '%')
                    OR u.name LIKE CONCAT('%', #{keyword}, '%')
                    OR COALESCE(p.product_name, rv.review_title, tu.name) LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </select> -->

</mapper>
