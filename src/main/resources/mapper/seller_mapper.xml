<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.suje.dao.SellerDAO">

    <!--가입-->
    <insert id = "insertSeller" 
            parameterType="seller">
        insert into sellers( user_id,
            company_name, business_number,  
            representative_name, opening_date, business_address)
        values( #{user_id}, #{company_name}, #{business_number}, 
                 #{representative_name}, 
                #{opening_date}, #{business_address} )
    </insert>
        

    <!--판매자 조회-->
    <select id="selectSeller" parameterType="int" resultType="seller">
        SELECT * FROM sellers WHERE user_id = #{user_id}
    </select>


    <!--판매자 업데이트-->
    <update id="sellerModify" parameterType="seller">
    UPDATE sellers
    SET company_name = #{company_name},
        business_number = #{business_number},
        representative_name = #{representative_name},
        opening_date = #{opening_date},
        business_address = #{business_address}
    WHERE user_id = #{user_id}
    </update>




    <select id="getOrderStatusCounts"
            parameterType="int"
            resultType="java.util.Map">
        SELECT 
            COUNT(DISTINCT CASE WHEN o.status = 'PAID' THEN o.order_id END) AS newOrderCount,
            COUNT(DISTINCT CASE WHEN o.status = 'PREPARING' THEN o.order_id END) AS preparingCount,
            COUNT(DISTINCT CASE WHEN o.status = 'SHIPPING' THEN o.order_id END) AS shippingCount,
            COUNT(DISTINCT CASE WHEN o.status = 'DELIVERED' THEN o.order_id END) AS deliveredCount,
            COUNT(DISTINCT CASE WHEN o.status = 'CANCELLED' THEN o.order_id END) AS cancelledCount
        FROM orders o
        JOIN order_items oi ON oi.order_id = o.order_id
        JOIN products p ON p.product_id = oi.product_id
        WHERE p.seller_id = #{seller_id}
    </select>

    <select id="getProductStatusCounts" 
            parameterType="int" 
            resultType="java.util.Map">
        SELECT 
            COUNT(CASE WHEN status = 'APPROVED' AND stock > 0 THEN 1 END) AS onSaleCount,
            COUNT(CASE WHEN status = 'APPROVED' AND stock = 0 THEN 1 END) AS outOfStockCount
        FROM products
        WHERE seller_id = #{seller_id}
    </select>

    <select id="getUnansweredQnaCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
        FROM qnas q
        JOIN products p ON p.product_id = q.product_id
        WHERE p.seller_id = #{seller_id}
        AND q.answered_at IS NULL
    </select>

    <select id="getNewReviewCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
        FROM reviews r
        JOIN products p ON p.product_id = r.product_id
        WHERE p.seller_id = #{seller_id}
    </select>

        <!-- 판매자 주문 목록 조회 -->
    <select id="getSellerOrderList"
            parameterType="map"
            resultType="order">
        SELECT DISTINCT
            o.order_id,
            o.user_id,
            o.total_amount,
            o.address_id,
            o.status,
            o.created_at,
            o.updated_at
        FROM orders o
        JOIN order_items oi ON oi.order_id = o.order_id
        JOIN products p ON p.product_id = oi.product_id
        WHERE p.seller_id = #{seller_id}

        <if test="status != null and status != ''">
            AND o.status = #{status}
        </if>

        ORDER BY o.order_id DESC
    </select>

    <!-- 판매자 주문별 상품 목록 조회 -->
    <select id="getSellerOrderItemList"
            parameterType="map"
            resultType="orderitem">
        SELECT
            oi.order_item_id,
            oi.order_id,
            oi.product_id,
            oi.price,
            oi.quantity,
            p.name AS productName,
            p.image_l AS imageL,
            (oi.price * oi.quantity) AS subtotalAmount
        FROM order_items oi
        JOIN products p ON p.product_id = oi.product_id
        WHERE oi.order_id = #{order_id}
        AND p.seller_id = #{seller_id}
        ORDER BY oi.order_item_id ASC
    </select>

    <!-- 판매자 주문 상태 변경 -->
    <update id="updateSellerOrderStatus"
            parameterType="map">
        UPDATE orders
        SET status = #{status},
            updated_at = NOW()
        WHERE order_id = #{order_id}
        AND EXISTS (
            SELECT 1
            FROM order_items oi
            JOIN products p ON p.product_id = oi.product_id
            WHERE oi.order_id = #{order_id}
            AND p.seller_id = #{seller_id}
        )
    </update>
</mapper>
