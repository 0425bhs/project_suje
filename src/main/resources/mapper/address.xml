<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.suje.dao.AddressDAO">


<!--가입-->
<insert id = "insertAddress" parameterType="address"  
useGeneratedKeys="true" keyProperty="address_id">
insert into addresses( user_id, address_name, recipient_name, phone, 
zipcode, address, detail_address, is_default )
values(#{user_id}, #{address_name}, #{recipient_name}, #{phone}, #{zipcode},
 #{address}, #{detail_address}, #{is_default} ) 
</insert>


<select id = "selectList" parameterType="int" resultType = "address">

select * from addresses where user_id = #{user_id}
</select>



</mapper>